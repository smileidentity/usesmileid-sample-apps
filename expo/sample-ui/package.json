{
  "name": "@smileid/sample-ui",
  "version": "0.0.0",
  "private": true,
  "description": "Every screen a partner sees, as pure TypeScript. Never published: the SDK repos consume this from source.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "sideEffects": false,
  "scripts": {
    "test": "TZ=UTC jest"
  },
  "peerDependencies": {
    "@expo/ui": ">=57.0.0",
    "@react-native-async-storage/async-storage": ">=1.21.0",
    "@smileid/usesmileid": ">=12.1.1",
    "react": ">=19.0.0",
    "react-native": ">=0.78.0",
    "react-native-gesture-handler": ">=2.0.0",
    "react-native-safe-area-context": ">=5.0.0",
    "react-native-svg": ">=15.0.0",
    "zustand": ">=5.0.0"
  },
  "devDependencies": {
    "@react-native/jest-preset": "0.86.3",
    "@testing-library/react-native": "14.0.1",
    "@types/jest": "29.5.14",
    "@types/node": "22.20.3",
    "@types/react": "19.2.18",
    "jest": "29.7.0",
    "jest-expo": "57.0.5",
    "test-renderer": "1.2.0",
    "typescript": "6.0.3"
  },
  "pinRationale": {
    "@smileid/usesmileid": "peer, not a dependency: the host resolves the SDK so one UI source compiles against the published artifact here and against SDK HEAD in the SDK repo. Nothing imports it yet — the flow host is the next tranche — but the peer contract is what the SDK repos substitute against.",
    "react": "peer at the floor the SDK itself declares, so the host's copy is the only React in the tree.",
    "react-native": "peer; the version travels with the host app, never with a library.",
    "react-native-svg": "peer; the design's marks are SVG and the SDK already requires this package.",
    "zustand": "peer; the stores are app-lifetime state and the host decides when they are created.",
    "@react-native/jest-preset": "Required by peer from both react-native and jest-expo, and matched to the react-native version.",
    "@testing-library/react-native": "Renders a component and reads its resolved styles, which is what the token assertions need.",
    "@types/jest": "Matched to the jest 29 family below.",
    "@types/react": "Matched to the react the host pins, or JSX types disagree between the two packages.",
    "jest": "29.7.0, not 30: jest-expo 57 depends on the jest 29 family (jest-snapshot, babel-jest, @jest/globals), and mixing a 30 runner with a 29 snapshot serialiser is how every golden reds at once.",
    "jest-expo": "Versioned with the Expo SDK; supplies the React Native transform and module mocks.",
    "test-renderer": "The modern replacement react-test-renderer became; @testing-library/react-native 14 requires it by peer.",
    "typescript": "6.0.3, the version Expo SDK 57 expects. It also satisfies typescript-eslint 8's `>=4.8.4 <6.1.0`, so type-aware linting stays on — 7.x would not.",
    "@types/node": "The test files read spec/ from disk, which needs the Node globals; nothing in src/ uses them.",
    "react-native-safe-area-context": "peer; the app bar reads the real status-bar inset rather than a constant, which is the rule insets stay platform-native.",
    "@expo/ui": "peer; the bottom sheet is the platform's own (SwiftUI sheet, Material3 ModalBottomSheet), which spec/components.json requires rather than a hand-rolled one. Already in the graph via expo-router.",
    "react-native-gesture-handler": "peer; the row swipe is the platform's own gesture, which spec/components.json requires rather than reproducing Android's velocity and threshold by hand.",
    "@react-native-async-storage/async-storage": "peer; the six settings are preferences and live where an uninstall clears them, apart from the token session. Already an optional peer of the SDK itself."
  }
}
