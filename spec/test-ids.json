{
  "version": 2,
  "description": "Accessibility ids this repo owns. Device flows and golden tests key off them, so they are a contract: stable forever, identical on all four platforms, and asserted by each app's spec-validation test. SDK ids (si_*) are NOT defined here — reference them from the SDK.",
  "prefix": "sample_",
  "ids": {
    "shell": [
      {
        "id": "sample_nav_products",
        "description": "Bottom nav destination: products."
      },
      {
        "id": "sample_nav_verifications",
        "description": "Bottom nav destination: verifications."
      },
      {
        "id": "sample_nav_settings",
        "description": "Bottom nav destination: settings."
      },
      {
        "id": "sample_nav_token",
        "description": "Bottom nav token affordance, carries the countdown ring."
      },
      {
        "id": "sample_scenario_drawer",
        "description": "The scenario drawer's own root, matching every other sheet.",
        "resolved": "RESOLVED 2026-08-14. This id stays on the drawer root: screens.json already lists it among the drawer's own testIds, every other sheet names its root the same way (sample_country_sheet, sample_profile_switch_sheet), and Android already attaches it there — so nothing is renamed, which the stability rule forbids. The control that opens it takes sample_scenario_drawer_button, matching every other opener (sample_country_trigger opens sample_country_sheet). Only this description was wrong."
      },
      {
        "id": "sample_scenario_drawer_button",
        "description": "Opens the scenario drawer. Automation-facing; the drawer is also reachable by deep link."
      },
      {
        "id": "sample_scenario_item",
        "description": "One drawer row; suffixed with the scenario id, e.g. sample_scenario_item_expiredToken."
      },
      {
        "id": "sample_theme_item",
        "description": "One theme row; suffixed with the theme scenario id."
      }
    ],
    "products": [
      {
        "id": "sample_products_screen",
        "description": "Products screen root."
      },
      {
        "id": "sample_env_chip",
        "description": "Environment chip. HIDDEN on the products header since 2026-08-24 (node 5447:1705 sets hidden=true) — the component survives and keeps this id, but no shipped screen renders it. Assert environment on sample_result_environment instead."
      },
      {
        "id": "sample_profile_avatar_button",
        "description": "Header avatar button; opens the profile-switch sheet (decision 2026-08-13)."
      },
      {
        "id": "sample_product_card",
        "description": "One product card; suffixed with the product id, e.g. sample_product_card_smartSelfieEnrollment."
      },
      {
        "id": "sample_session_card",
        "description": "Active token session card."
      },
      {
        "id": "sample_session_countdown",
        "description": "Remaining session time, as m:ss text."
      },
      {
        "id": "sample_session_ended_banner",
        "description": "Token session ended banner."
      },
      {
        "id": "sample_profile_switch_sheet",
        "description": "Switch-profile sheet root."
      },
      {
        "id": "sample_profile_switch_new",
        "description": "The switch sheet's last row; the profile it creates becomes active."
      }
    ],
    "verifications": [
      {
        "id": "sample_verifications_screen",
        "description": "Verifications list root."
      },
      {
        "id": "sample_filter_chip",
        "description": "One status filter chip; suffixed with the status, e.g. sample_filter_chip_attention."
      },
      {
        "id": "sample_filter_count",
        "description": "The count inside a chip; suffixed with the status. Asserted after deletes."
      },
      {
        "id": "sample_verifications_empty",
        "description": "Empty-list message on the verifications tab. Two texts behind one id: nothing submitted yet, versus nothing matching the active filter. Not in the design — added because the app seeds no rows, so a first launch would render a blank tab."
      },
      {
        "id": "sample_country_empty",
        "description": "Country search matched nothing. No supporting line: the reader typed the query."
      },
      {
        "id": "sample_idtype_empty",
        "description": "ID-type list is empty — either the country has no types, or the search matched none of them; the text says which."
      },
      {
        "id": "sample_job_row",
        "description": "One verification row, suffixed with N: its position in the list as drawn, with the active filter applied and the days newest first, counting from 0. A filter renumbers the rows it leaves."
      },
      {
        "id": "sample_job_row_status",
        "description": "Status badge inside a row."
      },
      {
        "id": "sample_select_toggle",
        "description": "Select / Cancel action in the header."
      },
      {
        "id": "sample_selection_checkbox",
        "description": "Row checkbox in select mode, suffixed with the same N as its sample_job_row."
      },
      {
        "id": "sample_selection_bar",
        "description": "Selection bar shown instead of the nav bar."
      },
      {
        "id": "sample_selection_count",
        "description": "'n selected' text."
      },
      {
        "id": "sample_selection_remove",
        "description": "Remove action in the selection bar."
      },
      {
        "id": "sample_toast",
        "description": "Toast container. Used by verifications (removal + undo) and by profiles (the created confirmation)."
      },
      {
        "id": "sample_toast_undo",
        "description": "The action inside a toast, whatever it is labelled: \"Undo\" on a removal, \"Make active\" on a created profile. The id is the contract, the label is not."
      }
    ],
    "verificationDetails": [
      {
        "id": "sample_verification_details_screen",
        "description": "Details screen root."
      },
      {
        "id": "sample_status_badge",
        "description": "Job status badge."
      },
      {
        "id": "sample_detail_field",
        "description": "One label/value row; suffixed with the field name, e.g. sample_detail_field_jobId."
      },
      {
        "id": "sample_detail_copy",
        "description": "Copy button on a field; suffixed with the field name."
      },
      {
        "id": "sample_details_delete",
        "description": "Delete action in the app bar."
      },
      {
        "id": "sample_details_empty",
        "description": "Shown when the route names a job this build has no row for, which a deep link can do. The jobId asked for is in the supporting line, because that is the whole diagnostic."
      },
      {
        "id": "sample_details_refresh",
        "description": "Pull-to-refresh container on the verification details screen, which is what the design draws in the processing state. Fetches GET /v3/status/{jobId} and rewrites the row. Always present, never conditionally hidden: a gesture that silently does nothing reads as a defect, so a refresh that cannot succeed reports why instead — no live scanned session, or a row never submitted under one (every fixture-token run). A row still processing is also refreshed automatically on entry, since that is the only status that can change."
      }
    ],
    "userDetails": [
      {
        "id": "sample_user_details_screen",
        "description": "User details screen root — shown BEFORE the SDK flow starts."
      },
      {
        "id": "sample_user_details_field",
        "description": "One editable row; suffixed with the field, e.g. sample_user_details_field_firstName. `_organisation` appears only while there is no profile, and names the one Continue creates."
      },
      {
        "id": "sample_user_details_hint",
        "description": "Requirement or edit hint line."
      },
      {
        "id": "sample_remember_details_switch",
        "description": "Whether Continue keeps the typed details: in the active profile, or as a new one when there is none. On by default. Once read \"Remember these details for next time\"; the id stays."
      },
      {
        "id": "sample_user_details_profile",
        "description": "Whose details these are: the active profile, or \"No profile yet\". Opens the switch sheet."
      },
      {
        "id": "sample_user_details_continue",
        "description": "Primary action that launches the SDK flow."
      }
    ],
    "kyc": [
      {
        "id": "sample_kyc_form_screen",
        "description": "KYC / ID details form root."
      },
      {
        "id": "sample_country_trigger",
        "description": "Country select trigger."
      },
      {
        "id": "sample_idtype_trigger",
        "description": "ID type select trigger."
      },
      {
        "id": "sample_idnumber_input",
        "description": "ID number input."
      },
      {
        "id": "sample_kyc_continue",
        "description": "Primary action."
      },
      {
        "id": "sample_token_float",
        "description": "Floating token-session affordance."
      },
      {
        "id": "sample_country_sheet",
        "description": "Country picker sheet root."
      },
      {
        "id": "sample_country_search",
        "description": "Country search field."
      },
      {
        "id": "sample_country_option",
        "description": "One country row; suffixed with ISO code."
      },
      {
        "id": "sample_idtype_sheet",
        "description": "ID type picker sheet root."
      },
      {
        "id": "sample_idtype_search",
        "description": "ID type search field."
      },
      {
        "id": "sample_idtype_option",
        "description": "One ID type row; suffixed with the type id."
      }
    ],
    "settings": [
      {
        "id": "sample_settings_screen",
        "description": "Settings root."
      },
      {
        "id": "sample_profile_summary",
        "description": "Profile row at the top of settings; opens the profiles LIST, not the active profile's page."
      },
      {
        "id": "sample_setting_enhanced_smart_selfie",
        "description": "Enhanced SmartSelfie™ switch → SelfieCaptureConfig.enableEnhancedLiveness, directly and not inverted: ON is the head-turn challenge, and it is ON by default for every product. RENAMED 2026-08-25 from sample_setting_smile_to_capture, whose label, polarity and default were all superseded by node 5206:2898 — a port still driving the old id is driving a row that no longer exists. It and agent mode write different fields but they CAN fight: FlowValidator.validateSelfie() raises AgentModeWithEnhancedLivenessException (BUILDER_AGENT_MODE_WITH_ENHANCED_LIVENESS) at ERROR severity when both are set, which blocks the build. With enhanced liveness now defaulting ON that pair is one tap away, so the host must hold a mutex: turning either row on turns the other off, enforced where settings are persisted rather than in the screen."
      },
      {
        "id": "sample_setting_agent_mode",
        "description": "Agent mode switch → SelfieCaptureConfig.allowAgentMode."
      },
      {
        "id": "sample_setting_dark_mode",
        "description": "Dark mode switch."
      },
      {
        "id": "sample_setting_consent_step",
        "description": "Consent screen switch → include/omit consent() in the flow."
      },
      {
        "id": "sample_setting_instructions_step",
        "description": "Instruction screen switch → include/omit instructions()."
      },
      {
        "id": "sample_setting_preview_step",
        "description": "Preview screen switch → include/omit preview()."
      },
      {
        "id": "sample_setting_nav",
        "description": "One ABOUT/LEGAL navigation row; suffixed with the row id."
      },
      {
        "id": "sample_licenses_screen",
        "description": "The third-party notices screen, opened by the LEGAL 'Open-source licenses' row."
      },
      {
        "id": "sample_licenses_empty",
        "description": "Shown when the generated notices asset did not ship with the build, which is the only way the list is empty."
      },
      {
        "id": "sample_license_row",
        "description": "One component's notice; suffixed with its artifact coordinate, with every non-alphanumeric character replaced by an underscore (a coordinate carries dots and colons, which an id may not)."
      },
      {
        "id": "sample_license_text",
        "description": "The expanded licence text for one component; same suffix as its row."
      },
      {
        "id": "sample_license_link",
        "description": "The row that opens a licence whose text ships inside the component rather than with the app; same suffix as its row."
      },
      {
        "id": "sample_sign_out",
        "description": "Sign out row."
      },
      {
        "id": "sample_sign_out_confirm",
        "description": "The confirmation's destructive action: ends the session and deletes every profile on the device. Expo shows the platform's own Alert, whose buttons take no id, so automation taps its label there."
      },
      {
        "id": "sample_version_label",
        "description": "App name and version footer."
      }
    ],
    "profiles": [
      {
        "id": "sample_profiles_screen",
        "description": "Profiles list root."
      },
      {
        "id": "sample_profile_row",
        "description": "One profile row; suffixed with the profile id."
      },
      {
        "id": "sample_create_profile",
        "description": "Create-profile row; opens the new-profile sheet, which returns to the list with the confirmation."
      },
      {
        "id": "sample_new_profile_sheet",
        "description": "New profile sheet root."
      },
      {
        "id": "sample_new_profile_name",
        "description": "Profile name input."
      },
      {
        "id": "sample_new_profile_first_name",
        "description": "First name input; required before Create profile enables."
      },
      {
        "id": "sample_new_profile_last_name",
        "description": "Last name input; required before Create profile enables."
      },
      {
        "id": "sample_new_profile_email",
        "description": "Email input, optional."
      },
      {
        "id": "sample_new_profile_phone",
        "description": "Phone input, optional."
      },
      {
        "id": "sample_new_profile_save",
        "description": "Create profile action."
      },
      {
        "id": "sample_profile_config_screen",
        "description": "Per-profile configuration root."
      },
      {
        "id": "sample_profile_config_field",
        "description": "One user-details default; suffixed with the field."
      },
      {
        "id": "sample_profile_config_callback_url",
        "description": "The profile's callback URL; empty means the partner's portal default. Disabled while a token session is live \u2014 that session's partner owns the job, so the profile's URL is not sent."
      },
      {
        "id": "sample_profile_config_save",
        "description": "The config page's one action. On the active profile it reads \"Save changes\" and is enabled once something changed; on any other it reads \"Use this profile\", saving any edits and making it active."
      },
      {
        "id": "sample_profile_config_name",
        "description": "The profile's organisation, which the SDK's consent screen names as the partner. Optional: blank names the app itself, never the person being verified."
      },
      {
        "id": "sample_profile_config_delete",
        "description": "Deletes the profile after a confirmation; the active one hands over to the first left, or to no profile."
      },
      {
        "id": "sample_profile_delete_confirm",
        "description": "The delete confirmation's destructive action. Expo shows the platform's own Alert, whose buttons take no id, so automation taps its label there."
      }
    ],
    "token": [
      {
        "id": "sample_scan_token_screen",
        "description": "Scan token screen root."
      },
      {
        "id": "sample_token_manual_entry",
        "description": "Manual token entry row."
      },
      {
        "id": "sample_token_paste",
        "description": "Paste action."
      },
      {
        "id": "sample_token_environment",
        "description": "One environment chip in the simulated-scan controls; suffixed with the environment id, e.g. sample_token_environment_sandbox. Which host the minted token's api_url names, so a flow picks an environment by minting rather than by tapping the words."
      },
      {
        "id": "sample_token_simulate",
        "description": "Simulate a successful scan — makes token flows testable with no QR source."
      }
    ],
    "resultCard": [
      {
        "id": "sample_result_card",
        "description": "The card container."
      },
      {
        "id": "sample_result_active_scenario",
        "description": "activeScenario field."
      },
      {
        "id": "sample_result_active_theme",
        "description": "activeTheme field."
      },
      {
        "id": "sample_result_route",
        "description": "route field."
      },
      {
        "id": "sample_result_environment",
        "description": "environment field. The only surface that publishes which environment a run submitted to, now that the token's api_url decides it and neither a setting nor a chip shows it."
      },
      {
        "id": "sample_result_job_id",
        "description": "jobId field."
      },
      {
        "id": "sample_result_user_id",
        "description": "userId field."
      },
      {
        "id": "sample_result_job_status",
        "description": "jobStatus field."
      },
      {
        "id": "sample_result_result_count",
        "description": "resultCallbackCount field."
      },
      {
        "id": "sample_result_refresh_count",
        "description": "refreshCallbackCount field."
      },
      {
        "id": "sample_result_last_error",
        "description": "lastError field."
      },
      {
        "id": "sample_result_sdk_version",
        "description": "sdkVersion field."
      }
    ]
  },
  "rules": [
    "One id per assertable element. If a flow needs to read it, it has an id.",
    "Suffixed ids derive from other spec files — product ids from scenarios.json products, scenario rows from scenarios.json, field names from result-card.schema.json — so adding a product or field adds its ids automatically.",
    "Never reuse an id for a different element, and never localise an id.",
    "Counters and countdowns are exposed as their value in text form, so a flow can assert equality rather than parse prose.",
    "Filter counts carry ids because 'the count dropped after a delete' is the assertion that proves the delete, not the toast."
  ]
}
