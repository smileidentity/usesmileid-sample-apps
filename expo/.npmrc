# React Native autolinking reads the app's own node_modules, so the SDK's peers must be real files there.
node-linker=hoisted
# A dropped peer is the consumption defect this repo exists to catch, so an unmet one fails the install.
strict-peer-dependencies=true
auto-install-peers=false
