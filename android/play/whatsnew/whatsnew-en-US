Profiles are saved on your device, encrypted. A fresh install has no profile: the details you type for your first job create one, and the form fills itself from it next time. Switch or add a profile straight from the details form, so each demo runs as the right person.

Every run now starts with empty ID details, so an ID number never carries over from the last run.
