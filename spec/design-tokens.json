{
  "version": 2,
  "designVersion": "2026-08-12",
  "description": "Design tokens for the four apps. The Smile ID design system is the single source; this file records where it lives, how each platform consumes it, and the deltas found when the design file was checked against it on 2026-08-12.",
  "source": {
    "name": "smile-design-system",
    "shape": "three-tier DTCG JSON — primitive → semantic → component — with pre-generated per-platform outputs",
    "rule": "Reference the highest tier that fits: component tokens when building that named component, semantic tokens otherwise. Never reference a primitive from app code, and never hardcode a hex.",
    "why": "The SDK's theme and these apps resolve from the same generated source, so the sample matches the SDK's branding without importing SDK components or copying values."
  },
  "verification": {
    "date": "2026-08-12",
    "method": "Figma variable definitions on the products board (node 5206-4037) compared against the design system's resolved light values.",
    "result": "Aligned. Every colour, the radius scale and the type family match exactly. The badge treatment was later resolved against the prototype recording — see the softBadgeFills delta.",
    "matches": [
      {
        "figma": "color/primary",
        "value": "#151f72",
        "token": "color.primary"
      },
      {
        "figma": "color/surface",
        "value": "#ffffff",
        "token": "color.surface"
      },
      {
        "figma": "color/title",
        "value": "#21232c",
        "token": "color.text.title"
      },
      {
        "figma": "color/muted",
        "value": "#848282",
        "token": "color.text.muted"
      },
      {
        "figma": "color/border",
        "value": "#eaecf0",
        "token": "color.border"
      },
      {
        "figma": "Main BG",
        "value": "#f9fafb",
        "token": "color.background"
      },
      {
        "figma": "radius/control",
        "value": "999 (pill)",
        "token": "radius.control"
      },
      {
        "figma": "Type/*",
        "value": "DM Sans 12–26",
        "token": "text-style.* (font-family.body)"
      }
    ]
  },
  "deltas": [
    {
      "id": "softBadgeFills",
      "severity": "blocks StatusBadge on all four statuses",
      "figma": {
        "color/warn-bg": "#fff0d9",
        "color/warn-fg": "#7a4a00"
      },
      "designSystem": {
        "badge.warning.background": "#ff9b00",
        "badge.warning.text": "#2d2b2a"
      },
      "finding": "CONFIRMED from the prototype recording 2026-08-12: the design uses SOFT tinted pills for ALL FOUR statuses (pale green Clear, pale amber Attention, pale red Blocked, pale blue Processing). The design system's badge.* tokens are saturated fills with inverse text, so no current token pair produces the intended treatment.",
      "recommendation": "Add soft badge variants to the design system (background + text per role). This is now a system gap rather than a per-status judgement call: the design applies soft fills consistently, and hardcoding the Figma hexes would bypass the token source.",
      "status": "OWNER DECISION 2026-08-13 — RESOLVED: keep the design's soft treatment and ADD soft badge variants to the design system (background + text per role for success/warning/error/info). Do not hardcode locally; the existing saturated pairs stay for surfaces that want them."
    },
    {
      "id": "darkBorder",
      "severity": "dark-mode defect in the token source",
      "designSystem": {
        "color.border light": "#eaecf0",
        "color.border dark": "#eaecf0"
      },
      "finding": "color.border does not change between light and dark. A #EAECF0 border on the dark background (#1A1C23) is near-maximum contrast and will read as a bright outline around every card and row. CONFIRMED ON A DEVICE 2026-08-13, the first time anything rendered these tokens in dark mode: the Android shell's nav-bar pill draws a bright near-white outline on #1A1C23, exactly as predicted. RE-CONFIRMED 2026-08-13 across the U1 primitives: every TextInput, SearchField and Toast outline, and every off/disabled Switch track, draws bright white on dark.",
      "rootCause": "semantic/dark/color.json DOES define border — it points at the same {color.grey.200} as light, and its own $description says 'SDK keeps stroke light at low opacity on dark; consumers may apply ~15% opacity'. So the intent is a translucent stroke, but the instruction is delegated to consumers: the generated per-platform output emits a fully opaque #EAECF0 and carries no opacity, and this repo's own rule forbids app-side colour arithmetic, so a consumer cannot legitimately apply it. The same is true of color.text.muted (see darkMuted).",
      "propagation": "12 tokens resolve through color.border and so inherit the defect — border, banner.border, card.border, filter.trigger-border, filter.popover-border, filter.chip-border, input.border, search.border, skeleton.highlight, table.border, table.row-border, tab.border — and 11 more through color.text.muted. They reference the semantic role correctly, so fixing the two semantic tokens fixes all 23. That makes this a high-leverage one-line fix, not 23 fixes.",
      "recommendation": "Resolve the dark value to the translucent colour the $description describes (e.g. rgba(234,236,240,0.15)) rather than delegating opacity to consumers — the generators already emit rgba() correctly. Do not patch it locally.",
      "status": "OWNER DECISION 2026-08-13 — RESOLVED: fix in the design system, not locally. Suggested values: color.border dark → a subtle dark-grey (e.g. #333846); color.text.muted dark → a lighter grey clearing 4.5:1 (e.g. #9BA1AC). Raise before U0 wires dark mode."
    },
    {
      "id": "darkMuted",
      "severity": "worth checking",
      "designSystem": {
        "color.text.muted light": "#848282",
        "color.text.muted dark": "#848282"
      },
      "finding": "Muted text is the same grey in both modes. On the dark background that lands near the 4.5:1 threshold for small text, and muted text is used for supporting lines and the date group headers.",
      "recommendation": "Ask for a lighter muted grey in dark mode, or verify the contrast ratio and accept it explicitly.",
      "status": "OWNER DECISION 2026-08-13 — RESOLVED: fix in the design system, not locally. Suggested values: color.border dark → a subtle dark-grey (e.g. #333846); color.text.muted dark → a lighter grey clearing 4.5:1 (e.g. #9BA1AC). Raise before U0 wires dark mode."
    },
    {
      "id": "pillRadius",
      "severity": "cosmetic",
      "figma": {
        "radius/control": "999"
      },
      "designSystem": {
        "radius.control → radiusPill": "32dp"
      },
      "finding": "Fully-rounded in Figma versus a fixed 32 in the generated output. Identical on a 48-tall button (anything ≥ 24 renders as a pill) but different on taller surfaces.",
      "recommendation": "No action for buttons. If a taller pill appears, treat 999 as the intent."
    },
    {
      "id": "composeTypeStylesAreComments",
      "severity": "emitter asymmetry — blocked U1 typography",
      "finding": "The Compose emitter writes all 29 type styles as COMMENTS, so MaterialTheme.typography is stock unless the app maps them itself. The Dart emitter produces 29 real TextStyles from the same resolved tokens.flat.json, and the SwiftUI emitter omits type entirely. This is an emitter asymmetry, not a platform limit: a Compose TextStyle needs no FontFamily to exist, and the emitter already holds every field it would need — build.mjs emitCompose() formats fontSize/fontWeight/lineHeight into a comment string and DROPS fontFamily and letterSpacing while doing it, so the comment is lossy as well as unusable.",
      "recommendation": "Ask upstream to make the Compose emitter emit real TextStyles, parameterised on font families the app supplies (the shape this repo generates is a working proposal). Until then scripts/sync_design_tokens.py generates them here, which keeps them generated rather than hand-copied and lets --check fail on staleness. Delete the generated file when upstream lands.",
      "status": "STOPGAP IN THIS REPO 2026-08-13 — android/sample-ui/.../tokens/SmileTypeStyles.kt is generated from the same source and mirrors the Dart emitter's names so the two stay diffable."
    },
    {
      "id": "composeDropsComponentDimensions",
      "severity": "affects every component's metrics",
      "finding": "emitCompose() and emitSwift() both filter dimensions to the top-level groups {spacing, radius, size, space, border-width}, so every COMPONENT dimension is dropped: button.height (52), button.padding-x/y, input.height (44), input.radius, search.height/gap, badge.radius/padding-x/padding-y, avatar.radius/size-sm/md/lg, card.radius/padding/gap. components.json names several of these as the tokens a primitive must use, and none of them exist in the Compose output.",
      "recommendation": "Ask upstream to emit component dimensions alongside component colours. Meanwhile the primitives reference the semantic scale token that carries the identical value (button.height → size.control-lg 52, input/search.height → size.control-md 44, input/search.radius → radius.field 12, button.radius → radius.control 32, badge.radius → radius.chip 999, avatar.radius → radius.full 999). avatar.size-md (40) has NO scale equivalent and falls back to space.40.",
      "note": "components.json describes Button as 48dp tall; the token source says button.height = 52px. The design system wins per the rules in this file, so the primitives use 52."
    },
    {
      "id": "buttonDisabledBypassesSemanticTier",
      "severity": "dark-mode defect in the token source",
      "designSystem": {
        "button.disabled.background": "{color.grey.200} — a PRIMITIVE reference",
        "button.disabled.text": "{color.text.muted}"
      },
      "finding": "tokens/component/button.json references the primitive {color.grey.200} for the disabled background instead of a semantic role, so it cannot re-resolve per mode — contradicting that file's own _comment, which states component tokens 'reference SEMANTIC roles only, so they re-resolve per light/dark automatically'. CONFIRMED ON A DEVICE 2026-08-13: the disabled Button draws a near-white #EAECF0 slab on the dark background. Distinct from darkBorder, and NOT fixed by fixing color.border.",
      "recommendation": "Point button.disabled.background at a semantic role that has a dark value. Do not patch locally."
    },
    {
      "id": "productHues",
      "severity": "blocks ProductCard on every product; the two document products are told apart by hue alone",
      "figma": {
        "node": "5206-4037 (Products · expressive), read 2026-08-15",
        "fillType": "linear-gradient at 124.169deg, stops at 13.397% and 86.603% — NOT the flat fills this spec previously assumed",
        "smartSelfieEnrollment": {
          "from": "#05723A",
          "to": "#0A9B4C"
        },
        "smartSelfieAuth": {
          "from": "#3A49B4",
          "to": "#5361D6"
        },
        "documentVerification": {
          "from": "#E08600",
          "to": "#FFB53D"
        },
        "enhancedDocumentVerification": {
          "from": "#2D2B2A",
          "to": "#4A4645"
        },
        "biometricKyc": {
          "from": "#151F72",
          "to": "#2B3A9E"
        },
        "enhancedKyc": "ABSENT — the design node predates the owner decision that added a sixth product, and still shows slot/empty in its place",
        "shadow": "each card carries its own hue as a tinted shadow: 0 10 18 -2 rgba(hue,0.24) + 0 2 5 0 rgba(hue,0.16)",
        "goPill": "rgba(255,255,255,0.16) on four cards but rgba(45,43,42,0.16) on Document Verification, whose amber is too light for a white scrim",
        "iconStroke": "the icon on the white tile is the card's start hue on three of four, but Document Verification's icon is #C46F00 rather than its #E08600 start — darkened for contrast, so the icon colour is not derivable from the gradient"
      },
      "designSystem": "Eight of the ten gradient stops do not exist in the token source at any tier. Only #2D2B2A (color.neutral.off-black / color.text.inverse) and #151F72 (color.primary) are present, and both are coincidences of value rather than a decorative role.",
      "finding": "VERIFIED against the design 2026-08-15, replacing the earlier stand-in. The product hues are raw gradient fills in the design file, bound to no variable — get_variable_defs on the products node returns only type, primary, muted, surface, title, border and the page background. So there is no token to consume and no generated output to read: implementing this today means ten hex literals in app code, which the repo's own rule calls a review failure, times four platforms.",
      "recommendation": "Add the product hues to the design system as a decorative role — a from/to pair per product, plus the sixth product the design node is missing. Until then treat this the same way as composeTypeStylesAreComments: generate the values from this spec entry rather than hand-copying them into four codebases. The mapping above is the single source all four apps read.",
      "status": "OPEN 2026-08-15. Android still renders the stand-in (the decorative palette indexed by declaration order) pending a decision on the stopgap."
    }
  ],
  "perPlatform": {
    "android": {
      "consume": "SmileTokens.kt — SmileColorLight / SmileColorDark / SmileDimens (SmileType is comments only); plus the generated SmileTypeStyles.kt for the type ramp",
      "wiring": "Feed the token objects into the app's Compose theme and select by isSystemInDarkTheme(); do not replace the SDK's own theme, which resolves the same tokens internally. Component tokens are grouped into the app's own mode-selected holder because SmileColorLight and SmileColorDark are two unrelated generated objects with no common supertype, so a component cannot choose between them itself.",
      "vendoredTo": "android/sample-ui/.../tokens/SmileTokens.kt (copied verbatim from dist/android)",
      "generatedHere": "android/sample-ui/.../tokens/SmileTypeStyles.kt — 29 real TextStyles, and the five DM Sans OFL faces in sample-ui/src/main/res/font/. Both come from scripts/sync_design_tokens.py --all and are covered by --check. The font is bundled rather than fetched through a downloadable-font provider: this file's rules say to ship it, and a provider would make text depend on Play Services and goldens depend on the network."
    },
    "ios": {
      "consume": "SmileTokens.swift — SmileColorLight / SmileColorDark / SmileSpacing",
      "wiring": "Select by colorScheme and expose through the app's environment.",
      "vendoredTo": "ios/SampleUI/Sources/SampleUI/Tokens/SmileTokens.swift (copied verbatim from dist/ios)"
    },
    "expo": {
      "consume": "tokens.ts",
      "wiring": "A typed token module the sample-ui package imports; no native code involved.",
      "vendoredTo": "expo/sample-ui/src/tokens.ts (copied verbatim from dist/ts)"
    },
    "flutter": {
      "consume": "flutter/sample_ui/lib/src/tokens/smile_tokens.dart — GENERATED (committed 2026-08-13)",
      "generatedBy": "scripts/sync_design_tokens.py, from the design system's platform-neutral dist/json/tokens.flat.json",
      "wiring": "Feed SmileColorLight / SmileColorDark into the app's ThemeData, selected by platform brightness.",
      "shape": "SmileColorLight (152) · SmileColorDark (152) · SmileDimens (42) · SmileType (29 TextStyles) · SmileShadows (6 BoxShadows) · SmileMotion (6 Durations)",
      "deliberateAdditions": "The upstream Compose emitter leaves typography as comments and omits shadows entirely. Dart expresses both directly, so SmileType emits real TextStyles and SmileShadows emits real BoxShadows. Naming otherwise mirrors Compose so the two are diffable. NOTE 2026-08-13: the typography half is an emitter gap rather than a Compose limitation — see deltas → composeTypeStylesAreComments. The shadow half is a genuine platform difference: Compose has no equivalent of an arbitrary offset/blur/colour shadow value.",
      "verified": "dart format canonical, generator idempotent, `dart analyze` clean in a Flutter package context, and colour membership identical to the Compose output.",
      "gap": false
    }
  },
  "rules": [
    "Tokens are generated, never hand-copied. A hex in app code is a review failure.",
    "Light and dark both come from the token source; no app-side colour arithmetic.",
    "Where the design file and the design system disagree, the design system wins and the disagreement is recorded in `deltas` above until the design team rules.",
    "Fonts: DM Sans for UI, the display family for headlines. Ship DM Sans with the apps rather than relying on a system fallback."
  ],
  "additions": {
    "comment": "Tokens this design needs that the system does not expose. Empty is the goal; each entry is a request to the design system, not a local override.",
    "candidates": [
      {
        "need": "product → hue and profile → hue mapping",
        "status": "OWNER DECISION 2026-08-13 — the DESIGNER supplies the list once (5 product hues now 6 with Enhanced KYC, plus the profile hues). Blocks final ProductCard and Avatar colours only; primitives and layout can proceed.",
        "shape": "productHues: {productId: color.decorative.*}, profileHues: {profileId: color.decorative.*}"
      }
    ]
  },
  "consumptionModel": {
    "decision": "OWNER DECISION 2026-08-13: vendor the tokens into this repo; do not change the design system.",
    "why": "The design system is distributed as an agent skill, not as a Maven / SPM / npm / pub package, so there is no dependency to declare. Vendoring its generated output is the only way to consume it on any platform.",
    "howToRefresh": "scripts/sync_design_tokens.py --all  (or --dart for Flutter only). Run --check in CI to fail when a vendored file is stale.",
    "parityGuard": "The script asserts that the generated Dart colour classes have the SAME MEMBERSHIP as the upstream Compose output, and prints the difference when they diverge. That guard caught two real bugs on first run: rgba() values silently dropped, and composite shadow colours wrongly emitted as standalone colours."
  }
}
